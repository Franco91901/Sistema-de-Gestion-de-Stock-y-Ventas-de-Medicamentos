spring:
  application:
    name: microservicio-notificaciones

  data:
    mongodb:
      uri: mongodb://localhost:27017/notificacionesdb

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8081